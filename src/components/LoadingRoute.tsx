import "./LoadingRoute.css";

const LoadingRoute = () => {
  // Loading page while retrieving project data.
  return (
    <div className="LoadingRoute">
      <h3>Loading Page...</h3>
    </div>
  );
};

export default LoadingRoute;
